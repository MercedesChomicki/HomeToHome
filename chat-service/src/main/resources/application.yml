spring:
  application:
    name: chat-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  config:
    import: optional:configserver:${SPRING_CLOUD_CONFIG_URI}

# Configuración JWT desde variable de entorno
jwt:
  secret: ${JWT_SECRET}